class SubscriptionsContainer{

    constructor(){
        this.subscribers = []; // lista de objetos con valores artista y email del suscriptor
    }

    addSubscriber(){
        //if artist does not exist 
        //throw error
    }

    removeSubscriber(){
 //if artist does not exist 
        //throw error
    }

    removeAllSubscriptionsToArtist(artistId){//se borran todos los emails suscriptos a un artista
 //if artist does not exist 
        //throw error
    }

} 


module.exports = {SubscriptionsContainer};